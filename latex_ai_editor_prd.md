# AI LaTeX Paper Editor 产品需求文档

> 产品代号：PaperCraft AI  
> 版本：v0.7.10 Workbench  
> 更新日期：2026-06-23  
> 当前形态：本地 Web App

---

## 1. 项目目标

构建一个面向科研论文 LaTeX 项目的 AI 写作增强工具。

用户可以导入完整论文项目，在 LaTeX 文本中选中任意内容，结合论文上下文调用大模型，获得多个带修改理由的学术润色版本，并由用户决定是否一键替换原文本。同时，系统可以对当前文本给出非侵入式写作反馈，且必须保持公式、命令、引用和专业术语不被破坏。

产品定位：**Cursor for Papers（论文版智能编辑器）**。

---

## 2. 核心用户

- PhD 学生
- 科研人员
- CFD、DRL、AI、机械、流体等工程科研用户
- 英文学术论文写作者

---

## 3. 核心价值

### 3.1 解决的问题

- LaTeX 局部修改效率低
- Grammarly 等工具不能理解 LaTeX 结构
- 使用通用聊天工具需要反复复制粘贴
- AI 改写容易破坏公式、引用和命令
- 论文项目包含多个 TeX、BibTeX 和图片文件，单文件编辑不足以理解项目结构

### 3.2 产品能力

- 选中即润色
- 自动携带上下文
- 保留 LaTeX 结构
- 提供多个改写版本
- 解释每个改写版本的修改理由
- 提供不自动改文的轻量写作反馈
- 一键替换并支持撤销
- 导入并浏览完整论文项目
- 查看参考文献文件和论文资源

---

## 4. MVP 用户流程

1. 用户双击 `启动论文编辑器.command`。
2. 本地服务启动并自动打开浏览器。
3. 系统自动恢复上次打开的完整论文项目；用户也可以选择新的论文文件夹、从 ZIP 创建项目，或打开单个可编辑文件。
4. 系统建立项目文件树并优先打开 `main.tex`。
5. 用户在编辑器中选中需要润色的内容。
6. 系统读取选区前后各 3 个段落作为上下文。
7. AI 返回保守修订、学术强化、精简表达三个版本，并解释每个版本的修改理由。
8. 用户复制结果或一键替换原选区。
9. 用户也可以点击“分析当前文件”查看 3-5 条写作反馈，反馈不自动修改文本。
10. 文本修改自动保存到浏览器项目缓存；拥有目录写权限时同时写回原文件。

---

## 5. 功能需求

### 5.1 本地启动

- 提供 macOS 双击启动文件
- 自动启动 Python 本地服务
- 自动在默认浏览器打开应用
- 重复启动时直接打开已运行的应用
- 关闭终端窗口或按 `Ctrl+C` 停止服务

### 5.2 项目导入

支持以下导入方式：

- 选择完整论文文件夹
- 从 ZIP 创建可见项目文件夹
- 导入单个可编辑文本文件或 `.docx` 文档
- 将单个可编辑文件拖入编辑区

入口职责：

- “打开文件”只用于打开单个可编辑文件，不再直接导入 ZIP
- ZIP 项目必须通过“从 ZIP 创建项目”导入并创建可见项目文件夹
- 选择或拖入 ZIP 时提示使用“从 ZIP 创建项目”
- `.docx` 仅提取正文为纯文本进行编辑，不保留 Word 样式、图片、批注、修订痕迹或版式
- 旧版 `.doc` 暂不支持，用户需另存为 `.docx` 或文本格式

ZIP 安全限制：

- 压缩包最大 50 MB
- 解压后总内容最大 100 MB
- 最多读取 500 个文件
- “从 ZIP 创建项目”由本地服务在应用目录 `projects/` 下创建 ZIP 同名子目录；重名时创建 `-2`、`-3` 等编号目录，不覆盖已有目录

### 5.3 项目文件树

- 显示项目中的相对路径和文件名
- 优先识别路径中的 `main.tex` 为主文件
- 没有 `main.tex` 时打开第一个 `.tex` 文件
- 支持在多个项目文件之间切换
- 切换文件前保留当前浏览器会话中的编辑内容

### 5.4 项目持久化与自动保存

- 使用 IndexedDB 保存完整项目状态，而不是只保存当前文本
- 保存项目名称、当前打开文件、全部可编辑文本和可预览资源
- 应用启动后自动恢复上次打开的项目和文件位置
- 按文件保存并恢复滚动位置、横向位置、光标和文本选区
- 首次打开没有历史状态的文本文件时必须定位到文件开头
- 文本发生修改后 600 ms 自动保存，不要求用户手动点击保存
- 顶部状态明确显示编辑中、保存中、保存完成或保存失败
- 通过“打开项目”取得目录读写权限时，自动写回原文本文件
- 通过“从 ZIP 创建项目”生成的项目，解压出的文本修改必须经本地服务自动写回 `projects/` 下的新建项目文件夹
- 单文件拖拽和普通文件选择器导入时，自动保存到浏览器本地项目缓存
- 浏览器不能取得写权限时，不得声称内容已写入原文件
- 图片和 PDF 资源保存在项目缓存中，以便恢复后继续预览

### 5.5 文件类型

可编辑文本：

- `.tex`、`.latex`
- `.bib`
- `.sty`、`.cls`、`.bst`、`.rtx`
- `.txt`、`.text`、`.md`
- `.docx`，仅提取正文纯文本，不保留 Word 格式

暂不支持：

- 旧版 `.doc` 二进制 Word 文件

可预览资源：

- `.pdf`
- `.png`、`.jpg`、`.jpeg`
- `.gif`、`.svg`、`.webp`

其他文件显示在文件树中，但当前不提供内容预览。

### 5.6 LaTeX 编辑器

- 支持纯文本 LaTeX 编辑
- 支持文本选区
- 显示行号、光标位置和词数
- 支持浏览器原生 undo/redo
- 自动保存当前项目与全部文本修改
- 支持在项目文本文件之间切换
- 长文本必须在编辑区域内部独立滚动，不能撑开页面布局
- 鼠标位于正文或行号栏时均可使用滚轮上下移动
- 超出编辑区宽度的 LaTeX 长行必须自动软换行，不要求横向滚动
- 自动换行只改变视觉布局，不得在源文件中插入真实换行符
- 编辑区宽度变化时，软换行必须实时重新排版
- 逻辑行换成多行显示后，行号必须保持纵向对齐

> 当前实现为轻量文本编辑器。Monaco Editor 仍作为后续升级项。

### 5.7 AI 润色

请求输入：

```json
{
  "text": "selected text",
  "context_before": "previous three paragraphs",
  "context_after": "next three paragraphs",
  "mode": "all | safe | academic | concise",
  "custom_prompt": "user-edited writing instruction"
}
```

返回结果：

```json
{
  "A": "conservative correction",
  "B": "strong academic rewrite",
  "C": "concise rewrite",
  "reasons": {
    "A": "short explanation",
    "B": "short explanation",
    "C": "short explanation"
  }
}
```

版本定义：

- A：保守修订，重点修复语法和清晰度
- B：学术强化，使用更严谨、正式的表达
- C：精简表达，减少冗余并保留原意

写作目标共四项：

- 全部版本：显示 A、B、C 三个结果
- 保守修订：仅显示 A 版本
- 学术强化：仅显示 B 版本
- 精简表达：仅显示 C 版本
- 每个返回版本可以包含一句修改理由，前端在结果卡片中显示
- 旧模型或旧接口未返回 reasons 时，前端仍必须正常显示 A/B/C 与 Diff

### 5.7.1 轻量写作反馈

- 后端提供 POST /api/feedback
- 请求字段包括当前文件路径、当前文件内容、可选选中文本、选区起止位置
- 返回字段为 feedback 数组，每条包含 type、severity、text 和 suggestion
- 写作反馈由用户手动点击“分析当前文件”触发，不做实时自动扫描
- 写作反馈的 text 和 suggestion 必须使用简体中文返回
- 每次返回 3-5 条反馈建议
- 反馈只提示表达、逻辑、过渡、结构或证据问题，不自动修改源码
- 当前 v1 不做行内高亮、源码定位或自动替换
- 当前文件过长时，后端优先截取当前光标或选区附近内容用于分析
- 未配置模型密钥时返回离线演示反馈

### 5.7.2 项目级 AI 对话

- 右侧结果区提供“对话”标签，用于围绕整个论文项目向大模型提问
- 后端提供 POST /api/chat
- 请求包含对话历史、当前文件路径、尽量完整的项目文本文件上下文和资源清单
- 默认尽量发送全项目文本；超过请求上限时优先完整保留当前文件、主 TeX、BibTeX 和 TeX 文件，其余文本截断并标记
- PDF、图片和未知二进制资源只作为路径、类型和大小清单提供，不发送内容
- 响应包含中文 reply，并可选返回 changes 数组
- changes 使用精确文本替换格式：path、find、replace、reason
- 模型不得直接改文件；所有 changes 必须先展示给用户确认
- 用户点击“应用这处修改”前，前端必须确认目标文本文件仍包含 find 原文
- 应用成功后更新项目文本、当前编辑器内容，并进入现有自动保存流程
- 当前 v1 不支持二进制文件编辑，不支持无确认自动写入

### 5.8 可编辑写作提示词

- AI 控制区提供多行写作提示词编辑框
- 全部版本、保守修订、学术强化、精简表达各自拥有独立默认提示词
- 通过下拉列表切换写作目标时自动显示该目标对应的提示词
- 用户可以自由编辑当前提示词，最多 4,000 个字符
- 用户编辑后的提示词立即保存到浏览器本地，并在下次启动时恢复
- 不同写作目标的自定义提示词必须分别保存，互不覆盖
- 润色请求必须携带当前编辑框的完整内容
- 用户编辑后的提示词是当前写作目标的最终风格要求，优先于系统内置风格描述
- 用户提示词不能覆盖 LaTeX 结构保护、JSON 返回格式和其他安全约束
- 提供“恢复默认”操作，且只重置当前写作目标

### 5.9 上下文系统

- 自动读取选区前 3 个段落
- 自动读取选区后 3 个段落
- 上下文仅用于辅助模型理解
- AI 只能返回选中部分的改写结果

### 5.10 LaTeX 与术语保护

模型提示必须保护：

- LaTeX 命令和环境
- 行内公式和块级公式
- `\cite{}`、`\ref{}`、`\label{}` 等引用结构
- CFD、LES、RANS 等技术缩写
- 用户原有变量名和符号

### 5.11 结果与替换

- 同时显示 A、B、C 三个结果
- 每个结果支持复制
- 每个结果支持一键替换原选区
- 每个结果可显示修改理由，但修改理由不得进入复制和替换文本
- 替换前检查原选区是否发生变化
- 替换操作进入浏览器撤销记录
- 编辑区产生有效选区后，将选区起止位置和原文锁定为论文选区状态
- 点击写作目标、润色按钮、结果区按钮或其他编辑区外控件不得清除论文选区
- AI 请求与一键替换必须使用锁定的论文选区，不依赖按钮点击后的浏览器焦点
- 只有用户再次点击、移动光标、输入或重新选择编辑区内容时，才更新或取消原论文选区
- 使用锁定选区前必须检查对应原文是否发生变化

### 5.12 模型接口

- 后端提供 `POST /api/rewrite`
- 后端提供 POST /api/feedback
- 模型密钥只在服务端读取
- 使用独立的 `model_config.json` 管理当前模型、接口地址和请求参数
- 每次润色请求前重新读取配置，修改模型信息后无需重启服务
- 使用 `active_provider` 在多个提供商配置之间切换
- 支持 OpenAI Chat Completions 兼容中转站及额外请求头
- 原生适配 Anthropic Messages API 和 Google Gemini generateContent API
- 内置 OpenAI、DeepSeek、通义千问、Kimi、智谱 GLM、硅基流动配置模板
- 支持全局超时、温度和最大输出 Token 配置
- API Key 可以直接配置，也可以使用 `env:变量名` 从环境变量读取
- 未配置当前提供商的 API Key 时使用离线演示模式
- `model_config.json` 必须加入 `.gitignore`
- 保留旧 `AI_API_KEY`、`AI_API_URL`、`AI_MODEL` 环境变量作为配置文件缺失时的兼容回退
- macOS 模型请求优先使用系统 `curl`，避免旧版 LibreSSL 与中转站 TLS/WAF 不兼容
- `curl` 请求不得在命令行参数中暴露 API Key
- 临时请求头文件权限必须为 `600`，并在请求结束后立即删除
- 系统无 `curl` 时回退到 Python urllib

中转站配置字段：

- `type`：使用 `openai_compatible`
- `api_key`：中转站密钥或 `env:变量名`
- `base_url`：服务根地址或完整 Chat Completions 地址
- `model`：中转站支持的模型名称
- `json_mode`：接口是否支持 JSON Mode
- `extra_headers`：中转站要求的额外请求头

### 5.13 逐词 Diff

- 每个 AI 结果显示原文与改写结果的逐词差异
- 删除内容使用红色删除标记，新增内容使用绿色新增标记
- Diff 保留空格、标点和 LaTeX 命令边界
- 大选区超过比较复杂度阈值时使用前后缀降级算法，避免阻塞界面
- Diff 只用于审核，不修改一键替换的最终文本

### 5.14 项目 ZIP 导出

- 支持把当前完整项目导出为 ZIP
- 导出使用当前编辑后的文本内容
- 保留图片、PDF、字体及未知二进制资源
- 生成的 PDF 可以随项目一同导出
- 服务端仅在内存中组装压缩包，不覆盖原项目
- 限制最多 500 个文件、解码后总量 100 MB

### 5.15 LaTeX 项目跳转

- 双击或按住 `Command/Ctrl` 点击 LaTeX 命令触发跳转
- `\cite{}`、`\citep{}`、`\citet{}` 跳转到 BibTeX 条目
- `\ref{}`、`\eqref{}`、`\autoref{}`、`\pageref{}` 跳转到 `\label{}`
- `\input{}`、`\include{}` 跳转到子 TeX 文件
- `\includegraphics{}` 打开对应图片或 PDF 资源
- 找不到目标时显示明确提示，不改变当前文件

### 5.16 搜索替换

- `Command/Ctrl+F` 打开当前文件搜索替换栏
- 支持上一处、下一处、替换当前和全部替换
- 显示当前匹配序号和总数
- 替换结果进入项目自动保存流程

### 5.17 本地 LaTeX 编译

- 后端自动检测 `latexmk`、`xelatex` 或 `pdflatex`
- 项目仅复制到本机临时目录编译，不上传外部服务
- 禁止 shell-escape，完整构建流程最长 90 秒
- 优先使用 `latexmk` 处理多轮编译和参考文献
- 直接使用 XeLaTeX 或 PDFLaTeX 时，检测参考文献并自动执行 BibTeX，随后再排版两次
- 编译错误解析为文件、行号和消息，点击后跳回源码
- 编译成功后生成 PDF，并把 SyncTeX 产物保留在响应中
- PDF 通过本地临时 HTTP 地址提供，支持 HEAD 与 Range 分段读取；前端加载前验证地址可用，失效时回退到本次编译生成的本地 Blob 缓存
- 通过“打开项目”导入的 PDF、图片等二进制资源在编译或导出前优先从文件句柄重新读取；保存项目状态时必须把二进制资源真实内容持久化到 IndexedDB，文件句柄不可用时回退到该资源缓存，避免每次重启后要求重新打开项目
- 二进制资源读取失败且没有缓存可用时必须显示中文可操作提示，说明需要重新打开项目文件夹，不得直接展示 `The object can not be found here`
- 未安装工具链时返回 `toolchain_missing`，不得显示虚假成功状态
- 检查系统 PATH、`/Library/TeX/texbin`、Homebrew、本项目 `tools` 目录和 `LATEX_ENGINE`
- 旧服务返回 404 或非 JSON 内容时，前端必须提示重启服务，不得显示底层对象错误

### 5.18 PDF 预览

- 右侧结果区提供 AI、PDF 与对话三个标签
- 手动编译成功后直接切换到 PDF 标签并显示结果，不弹出编译诊断面板
- 编译失败、工具链缺失或存在需要展示的诊断时，使用编译诊断面板显示错误并支持源码定位
- 支持保存后延迟自动编译并刷新 PDF，自动编译开关常显在编译按钮上方并与编译按钮组成右对齐两行操作组
- PDF 预览地址会加时间戳避免旧缓存；本地 `/api/pdf/` token 失效时自动切换到本次编译响应里的本地缓存，不显示对象不存在页面
- PDF 预览区提供“下载 PDF”和“全屏浏览”按钮；下载使用本次编译生成的 Blob 缓存，全屏浏览直接在新标签打开浏览器独立 PDF 预览器，并请求 250% 缩放，避免 CSS 放大造成清晰度下降
- 手动编译先切换到可见 PDF 标签再加载 iframe；后台自动编译若 PDF 标签未打开，则暂存最新预览地址，等用户打开 PDF 标签后再加载，避免隐藏 iframe 触发浏览器 PDF 对象错误
- 浏览器 PDF 预览加载异常不得覆盖编译成功状态；前端捕获 iframe 加载异常并显示中文提示，避免把底层英文 DOM 错误作为编译失败提示
- 自动编译默认关闭，缺少工具链时自动关闭
- 当前只完成 PDF 实时刷新；基于 SyncTeX 的源码与 PDF 双向定位尚未完成

---

## 6. 页面结构

### 6.1 顶部工具栏

- 产品名称
- 中文/英文界面切换
- 当前文件名
- 打开文件
- 打开项目文件夹
- 保存状态
- 撤销、重做
- 润色选区
- 导出 ZIP

### 6.2 左侧：项目与编辑器

- 项目文件树
- LaTeX 文本编辑器
- PDF 和图片资源预览
- 行号、光标位置、词数
- 文件树与正文之间提供可拖拽分隔条
- 搜索替换工具栏
- 编译 PDF 与错误定位入口
- 保存后自动编译开关与更醒目的编译 PDF 主按钮组成右对齐两行操作组
- `Command/Ctrl` 点击 LaTeX 引用与资源跳转

### 6.3 中间：AI 控制面板

- 写作目标下拉选择
- LaTeX 结构默认强制保护，不提供关闭开关
- 开始润色按钮
- 上下文状态说明
- 位于“上下文已启用”下方的当前写作目标可编辑提示词
- 提示词字符计数和恢复默认按钮
- 左右边界可拖拽调整

### 6.4 右侧：润色结果

- AI 状态
- A/B/C 三个结果卡片
- 四个写作目标选项：全部版本、保守修订、学术强化、精简表达
- 复制按钮
- 替换选区按钮
- 逐词 Diff 审核视图
- AI/PDF/对话标签切换
- 编译后 PDF 实时预览
- 项目级对话消息、上下文状态和建议修改预览

### 6.5 可调整布局

- 编辑区、AI 控制区和结果区之间提供可拖拽分隔条
- 项目文件树与正文编辑器之间提供可拖拽分隔条
- 拖动过程中正文根据可用宽度实时自动换行
- 为各区域设置最小宽度，避免内容被拖动到不可操作
- 用户调整后的区域宽度保存在浏览器本地，下次启动自动恢复

### 6.6 界面语言

- 顶部工具栏提供 `中 / EN` 语言切换按钮
- 支持中文和英文界面文案切换，选择保存到浏览器本地
- 切换范围包括按钮、标题、标签、输入占位、主要状态、空态、PDF 预览提示、聊天区和前端生成的操作提示
- 语言切换不得翻译论文内容、项目文件名、AI/model 返回内容、编译日志、诊断原文或用户自定义写作提示词
- 语言切换不得重置项目、清空选区、触发保存、重新请求 AI 或重新编译

---

## 7. 当前完成状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| macOS 双击启动 | 已完成 | 自动启动服务并打开浏览器 |
| 后端分层架构 | 已完成 | `server.py` 仅保留启动入口，HTTP、项目 IO、LaTeX 编译、PDF 缓存、模型配置、模型客户端和 AI 工作流已拆分到 `papercraft/` 模块 |
| 前端分层架构 | 已完成 | 前端入口改为 `static/app.js` ES Module，状态、缓存、文件、编辑器、LaTeX 跳转、编译、PDF 预览、AI 润色、反馈、对话和布局已拆分为 `static/` 功能模块 |
| 界面双语切换 | 已完成 | 顶部 `中 / EN` 按钮可在中文和英文 UI 间切换，选择持久保存；不翻译论文内容、AI 输出、编译日志或项目文件 |
| 单文件导入 | 已完成 | “打开文件”只支持单个可编辑文件，支持 Tex、Latex、Bib、样式类、Txt、Text、Markdown 和 `.docx` 纯文本提取 |
| 文件夹导入 | 已完成 | 通过浏览器目录选择器读取 |
| ZIP 导入 | 已完成 | 仅通过“从 ZIP 创建项目”创建可见项目文件夹，不再由“打开文件”直接导入 |
| 从 ZIP 创建项目文件夹 | 已完成 | 在应用目录 `projects/` 下创建 ZIP 同名可见项目目录，重名自动编号且不覆盖 |
| 项目文件树 | 已完成 | 支持文件切换和主文件识别 |
| BibTeX 与 LaTeX 辅助文件查看编辑 | 已完成 | BibTeX、样式、类、BST 与 RTX 均作为文本文件处理 |
| PDF/图片预览 | 已完成 | 浏览器内预览 |
| 文本选择与替换 | 已完成 | 含选区变化检查 |
| 三版本润色接口 | 已完成 | 支持演示模式和兼容模型接口 |
| 修改理由显示 | 已完成 | A/B/C 可显示简短理由，旧返回格式兼容 |
| 轻量写作反馈 | 已完成 | 手动分析当前文件，反馈只提示不改文 |
| 项目级 AI 对话 | 已完成 | 右侧对话标签，建议修改需确认后应用 |
| 上下文提取 | 已完成 | 前后各 3 个段落 |
| 完整项目自动恢复 | 已完成 | 使用 IndexedDB 保存文本与资源 |
| 修改后自动保存 | 已完成 | 600 ms 防抖并显示真实保存状态 |
| 写回原项目 | 部分完成 | 目录授权导入可写回；ZIP 和普通文件保存到缓存 |
| 编辑视图恢复 | 已完成 | 按文件恢复滚动、光标和选区 |
| LaTeX 长行软换行 | 已完成 | 随编辑区宽度实时重新排版 |
| 可拖拽区域布局 | 已完成 | 文件树和三大工作区均可调整宽度 |
| 布局宽度恢复 | 已完成 | 使用浏览器本地存储保存 |
| 论文选区锁定 | 已完成 | 编辑区外操作不取消选区 |
| 独立模型配置文件 | 已完成 | 请求前动态读取 model_config.json |
| OpenAI 兼容中转站 | 已完成 | 支持自定义地址、模型和请求头 |
| 多模型原生适配 | 已完成 | Claude、Gemini 及常见兼容提供商 |
| 中转站 TLS 兼容传输 | 已完成 | curl 优先、urllib 跨平台回退 |
| 可编辑写作提示词 | 已完成 | 按目标独立保存并随请求发送 |
| 逐词 Diff | 已完成 | 新增、删除与大选区降级比较 |
| 完整项目 ZIP 导出 | 已完成 | 文本和二进制资源均保留 |
| 引用、标签与资源跳转 | 已完成 | Cite、Ref、Input、Graphics |
| 搜索替换 | 已完成 | 当前文件导航与批量替换 |
| 本地 LaTeX 编译适配 | 已完成 | 可用即编译，缺失时结构化诊断 |
| 编译错误源码定位 | 已完成 | 文件与行号跳转 |
| PDF 实时刷新 | 已完成 | 手动和保存后自动编译；编译控件以右对齐两行操作组显示，成功不弹窗，失败弹出诊断；PDF 地址失效时自动回退本地缓存；预览区支持下载和全屏浏览 |
| SyncTeX 双向定位 | 未开始 | 已保留编译产物，尚未实现映射 |
| Monaco Editor | 未开始 | 当前使用轻量文本编辑器 |
| LaTeX 语法高亮 | 未开始 | 依赖后续编辑器升级 |
| 大文件增量渲染 | 未开始 | 当前行号和软换行仍为全量计算 |

---

## 8. MVP 范围

### 8.1 本阶段包含

- 本地 Web App
- 论文项目导入
- 文件树与资源预览
- LaTeX 文本编辑和选区
- AI 局部润色
- AI 修改理由
- 轻量写作反馈
- 项目级 AI 对话
- 上下文提取
- 多版本结果
- 一键替换
- 完整项目持久化与启动恢复
- 文本修改自动保存
- 逐词 Diff
- 项目 ZIP 导出
- 引用、标签、子文件和资源跳转
- 搜索替换
- 本地编译适配、错误定位和 PDF 实时刷新
- 中文/英文界面切换

### 8.2 本阶段不包含

- 全文自动生成
- PDF 内容解析
- 自动生成 Citation
- SyncTeX 源码与 PDF 双向定位
- LaTeX 语法高亮和语言服务器诊断
- 覆盖写回 ZIP 或普通文件选择器导入的原始文件
- Git 或云端协作

---

## 9. 下一阶段优先级

1. 接入 Monaco Editor、LaTeX 语法高亮和大文件增量渲染。
2. 使用 SyncTeX 实现源码与 PDF 双向定位。
3. 增加跨文件全项目搜索和重命名。
4. 增加可配置的术语表和禁止修改列表。
5. 增加编译引擎、主文件和构建参数设置。

---

## 10. 验收标准

- 双击启动器后能自动打开应用
- 能导入包含 Tex、BibTeX、PDF 和图片的论文文件夹
- “打开文件”只打开单个可编辑文件，不直接导入 ZIP
- 选择或拖入 ZIP 时提示使用“从 ZIP 创建项目”
- 能从 ZIP 创建可见项目文件夹，重名时自动创建编号目录且不覆盖已有项目
- 能打开 `.docx` 并以纯文本载入正文内容
- 选择旧版 `.doc` 时显示暂不支持提示
- 点击 `中 / EN` 后，主要界面按钮、标题、标签、占位符、状态、空态、PDF 和聊天区文案能在中文与英文之间切换
- 刷新应用后保留上次选择的界面语言，且不改变论文内容、项目文件名、AI 输出、编译日志或用户提示词
- 文件树能显示并打开支持的文件
- `main.tex` 存在时能被优先打开
- 选中文本后能获得三个润色版本，并在模型支持时显示每个版本的修改理由
- 替换不会修改选区之外的内容
- 点击“分析当前文件”后能显示 3-5 条写作反馈，且不会自动修改源码
- 右侧“对话”标签能围绕项目上下文回答问题
- 项目级对话生成的 changes 必须先展示预览，不得自动写入文件
- 点击应用建议修改前必须检查目标文件仍包含原 find 文本
- 未配置模型密钥时仍能完成演示流程
- 模型密钥不会发送到浏览器端
- 打开长篇 `.tex` 文件后，正文和行号能同步、连续地上下滚动
- 关闭并重新启动应用后，能自动恢复上次项目、当前文件和项目资源
- 修改任意可编辑文本后，无需手动操作即可保存到项目缓存
- 通过目录写权限打开的项目，修改后能自动写回对应原文件
- 从 ZIP 创建的项目修改后能自动写回新建项目文件夹
- 无原文件写权限时，状态栏只能显示本地缓存已保存
- 首次打开文本文件时显示文件开头，不得自动滚动到文末
- 切换文件或重启应用后，恢复该文件上次的滚动位置、光标和选区
- 单段 LaTeX 内容很长时，在编辑区内自动软换行且不产生横向滚动
- 拖动区域分隔条时，正文软换行实时响应且源文本不发生变化
- 软换行后，逻辑行号与对应文本首行保持对齐
- 重启应用后，各区域恢复用户上次调整的宽度
- 选中文本后切换四个写作目标，编辑区选区保持不变
- 点击润色及结果区按钮时，AI 请求和替换仍使用原选区
- 重新点击或移动编辑区光标后，旧选区才被取消或更新
- 修改 `model_config.json` 的当前提供商后，无需重启即可用于下一次请求
- OpenAI 兼容中转站能通过自定义地址、模型、密钥和额外请求头调用
- Claude 和 Gemini 配置分别生成符合其原生协议的认证头与请求体
- 未填写当前提供商密钥时进入离线演示模式，且密钥不会进入浏览器响应
- 在 macOS 旧版 LibreSSL 环境下，中转站请求能通过系统 curl 正常完成
- curl 进程参数和错误日志不得包含 API Key，临时请求头文件请求结束后必须删除
- 选择全部版本时显示 A/B/C，选择单项写作目标时只显示对应结果
- 通过下拉列表切换四个写作目标时，编辑框显示并保留各自独立的提示词
- 修改提示词后刷新应用，内容仍能恢复
- AI 请求使用用户编辑后的提示词作为当前目标的最终风格要求
- 恢复默认只影响当前目标，不修改其他目标的提示词
- 自定义提示词不得绕过 LaTeX 结构保护或接口返回格式
- AI 结果能显示逐词新增和删除，且 Diff 不改变替换文本
- AI 修改理由只用于解释建议，不得进入一键替换结果
- 导出的 ZIP 能还原当前 Tex、Bib、RTX、图片、PDF 和未知二进制资源
- 点击 Cite、Ref、Input 和 IncludeGraphics 能定位到对应定义或资源
- 搜索替换能显示匹配数，并把替换结果进入自动保存
- 未安装 TeX 工具链时编译返回明确安装提示
- 未生成 PDF 时 PDF 标签只显示安装提示，不加载空对象
- 安装工具链后编译使用临时目录、禁用 shell-escape，并返回 PDF 或结构化错误
- 含 BibTeX 参考文献的项目完成引擎、BibTeX 和两轮重排，总耗时不超过 90 秒
- 编译错误可以点击跳回对应源文件和行号
- 编译成功后右侧 PDF 标签刷新且不弹出诊断面板，自动编译可由用户在编译按钮上方常显开关
- PDF 生成后，用户可以从预览区直接下载当前 PDF，或在新标签以 250% 缩放打开独立 PDF 预览器
- 编译失败、工具链缺失或编译服务异常时显示诊断面板，错误可以点击跳回对应源文件和行号
- Safari 能通过本地 PDF 地址显示编译结果；本地地址失效、被淘汰、PDF 标签原本隐藏或目录资源旧快照失效时，不出现对象不存在提示，也不会在资源缓存可用时要求重新打开项目

---

## 11. 变更记录

### v0.7.10 - 2026-06-23

- 新增顶部 `中 / EN` 界面语言切换，支持中文和英文 UI 文案
- 语言选择保存到浏览器本地，刷新后保持上次选择
- 本次仅切换产品界面按钮、标题、标签、占位、状态、空态和前端操作提示，不翻译论文内容、项目文件名、AI/model 输出、编译日志、诊断原文或用户自定义提示词
- 后端 API、IndexedDB 数据、localStorage 既有键、项目导入导出、编译、PDF 预览、润色、反馈和对话行为保持兼容
- README 更新为分节中英双语说明，并同步当前单文件打开、ZIP 项目创建、`.docx` 纯文本导入、界面语言切换、LaTeX 编译和模型配置行为

### v0.7.9 - 2026-06-23

- “打开文件/ZIP”入口改为“打开文件”，职责收敛为只打开单个可编辑文件，不再直接导入 ZIP 项目
- ZIP 项目导入统一改为使用“从 ZIP 创建项目”，选择或拖入 ZIP 时提示用户使用该入口
- 单文件导入扩展 `.text`，并新增 `.docx` 纯文本提取；旧版 `.doc` 暂不支持，需另存为 `.docx` 或文本格式
- 后端新增文档导入接口，使用 Python 标准库读取 `.docx` 正文并返回可编辑纯文本，不保留 Word 样式、图片、批注、修订痕迹或版式

### v0.7.8 - 2026-06-23

- 前端从根目录单文件 `app.js` 拆分为 `static/` ES Modules：启动入口、共享状态、IndexedDB、文件导入导出、编辑器、LaTeX 跳转、编译、PDF 预览、AI 润色、写作反馈、项目对话和布局分别独立维护
- `index.html` 改为通过 `type="module"` 加载 `static/app.js`，根目录 `app.js` 保留为不再加载的兼容说明文件
- 本次为兼容性架构重组，现有 UI、DOM id/class、IndexedDB/localStorage key、后端 `/api/*` 请求格式、AI、反馈、对话、编译、PDF 预览、导入导出和自动保存行为保持不变

### v0.7.7 - 2026-06-23

- 后端从单文件 `server.py` 拆分为 `papercraft/` 分层模块：HTTP 路由、项目 IO、LaTeX 编译、PDF token 缓存、模型配置、模型客户端、AI 润色、写作反馈、项目对话和系统提示词分别独立维护
- `server.py` 调整为本地服务启动入口，继续使用 `ThreadingHTTPServer` 监听 `127.0.0.1`，并保留 `PORT` 与 `AUTO_OPEN` 启动行为
- 本次为兼容性架构重组，现有 `/api/*` 路由、请求/响应字段、错误文案、模型配置、离线演示、LaTeX 编译、PDF Range 预览、ZIP 项目创建、项目写回和导出行为保持不变
- 服务版本和双击启动脚本版本同步升至 0.7.7，确保启动脚本能识别并重启旧版 PaperCraft 服务

### v0.7.6 - 2026-06-23

- 新增“从 ZIP 创建项目”工作流：用户选择 ZIP 后，本地服务在应用目录 `projects/` 下创建 ZIP 同名可见项目文件夹，重名时自动编号并以真实目录模式载入，后续文本修改可写回新建文件夹
- 修复编译或导出前读取项目 PDF/图片资源时，浏览器旧 File 快照失效会弹出 “The object can not be found here” 的问题：目录导入资源会优先通过文件句柄刷新，并在保存项目状态时把真实字节写入 IndexedDB，重启后可从持久化资源缓存恢复
- 修复 REVTeX `.rtx` 辅助文件被误判为未知资源的问题：文件夹和 ZIP 导入现在都把 `.rtx` 按文本处理，重启恢复后可直接参与编译
- 二进制资源读取失败且没有缓存可用时改为中文可操作提示，要求重新打开项目文件夹，不再把底层英文 DOM/File 异常作为编译报错展示
- PDF 预览区新增“下载 PDF”和“全屏浏览”按钮，下载使用本次编译 Blob，全屏浏览直接打开浏览器独立 PDF 预览器并请求 250% 缩放，避免 CSS 放大造成清晰度下降
- `index.html` 为 `app.js` 更新版本参数，启动脚本和服务版本为 0.7.6，确保旧前端缓存和旧服务会被刷新

### v0.7.5 - 2026-06-23

- 修复 PDF iframe 在隐藏标签中加载时仍可能显示 “The object can not be found here” 的问题：手动编译先切换到 PDF 标签再加载，自动编译在 PDF 标签未显示时延迟加载
- `index.html` 为 `app.js` 增加版本参数，避免浏览器继续使用旧前端脚本导致修复不生效
- PDF 预览阶段异常与 LaTeX 编译结果解耦：即使浏览器内置 PDF 查看器加载失败，也只显示中文预览提示，不再弹出底层英文对象错误

### v0.7.4 - 2026-06-22

- 加固 PDF 预览加载：前端先用 HEAD 验证 `/api/pdf/` 地址并追加时间戳，地址不可用时自动回退到本次编译响应生成的本地 Blob 缓存
- 后端编译 PDF token 缓存改为 30 分钟 TTL、最多 32 份并按时间淘汰，降低自动编译和浏览器 Range 分段请求触发对象不存在的概率
- 启动脚本和服务版本升至 0.7.4，确保双击启动时会重启旧版 PaperCraft 服务并加载新的 PDF 预览逻辑

### v0.7.3 - 2026-06-22

- 编译结果新增本地 `/api/pdf/` 预览地址，并支持 HEAD、Range 和 inline PDF 响应
- 修复 Safari 使用临时 Blob 地址预览时显示 “The object can not be found here” 的问题
- 新增 `/api/health` 版本信息，双击启动脚本会识别并重启旧版 PaperCraft 服务
- 手动编译成功后不再弹出诊断面板，改为直接切换 PDF 预览并显示轻量提示；自动编译开关移到编译按钮上方常显
- 编译区 UI 调整为右对齐两行操作组：上方保存后自动编译胶囊开关，下方高对比渐变编译 PDF 主按钮；前端样式表使用版本参数避免旧样式缓存
- AI 润色结果新增修改理由显示，后端兼容旧 A/B/C 返回格式
- 新增手动触发的写作反馈卡片和 POST /api/feedback，返回 3-5 条非侵入式建议且不自动改文
- AI 控制区删除当前选区卡片，写作目标改为紧凑下拉列表，写作提示词移动到上下文说明下方，写作反馈要求中文返回
- AI 控制区移除 LaTeX 结构保护开关，服务端默认强制保护公式、命令、引用、标签和术语
- 右侧结果区新增“对话”标签和 POST /api/chat，可携带项目上下文进行中文问答，并生成需用户确认后应用的精确文本替换建议

### v0.7.2 - 2026-06-22

- 在当前开发机安装并验证 BasicTeX 2026，服务可从 `/Library/TeX/texbin` 自动检测 XeLaTeX
- 补齐示例论文所需的 REVTeX 4.1、CJK、newtx、overpic、epic 等宏包依赖
- 直接引擎编译新增 BibTeX 检测与两轮重排，完整构建共享 90 秒超时
- 使用当前 `main.tex` 和项目资源完成真实 XeLaTeX、BibTeX 与两轮重排验证，生成 15 页 PDF、24 条参考文献且无未解析引用

### v0.7.1 - 2026-06-22

- 修复编译接口 404/非 JSON 响应显示为底层对象错误的问题
- 未生成 PDF 时不再加载空 iframe 对象
- 扩展 MacTeX、BasicTeX、Homebrew、项目 tools 和 LATEX_ENGINE 路径检测
- Tectonic 0.16.9 在当前 macOS 26 环境崩溃或挂起，已从自动检测中移除
- 明确未安装工具链时无法生成 PDF

### v0.7 - 2026-06-22

- 新增逐词 Diff，显示 AI 新增与删除内容，并为大选区提供性能降级
- 新增完整项目 ZIP 导出，保留文本和所有二进制资源
- 新增 Cite、Ref、Input、Include 和 IncludeGraphics 项目跳转
- 新增当前文件搜索替换、匹配导航和全部替换
- 新增本地 LaTeX 编译适配、90 秒限制、shell-escape 禁用和错误源码定位
- 新增 AI/PDF 右侧标签、手动编译预览和保存后自动刷新
- 明确 SyncTeX 双向定位、语法高亮和大文件增量渲染仍属于下一阶段

### v0.6 - 2026-06-22

- 在 AI 控制区新增可编辑写作提示词文本框
- 为四个写作目标提供独立默认提示词并按目标切换
- 用户编辑内容自动保存到浏览器本地并优先作为最终写作要求
- 新增提示词字符计数和当前目标“恢复默认”操作
- 服务端新增 4,000 字符校验，并保持 LaTeX 保护与 JSON 格式为不可覆盖约束

### v0.5.2 - 2026-06-22

- 写作目标由三项补全为四项
- 新增独立的“保守修订”选项并映射到 A 版本
- 单项写作目标只显示对应结果，全部版本继续显示 A/B/C

### v0.5.1 - 2026-06-22

- 修复 macOS Python LibreSSL 2.8.3 访问部分中转站时出现 403/1010 或 TLS EOF 的问题
- 模型请求改为优先使用系统 curl，并保留 urllib 跨平台回退
- API Key 通过权限为 600 的临时请求头文件传递，请求结束后立即删除
- 已使用配置的中转站和 `gpt-5.5` 完成真实 HTTP 200 连通性验证

### v0.5 - 2026-06-21

- 新增独立 `model_config.json` 模型配置文件并支持请求前动态重载
- 新增 OpenAI 兼容中转站配置及自定义额外请求头
- 原生适配 Anthropic Claude 与 Google Gemini 接口
- 内置 OpenAI、DeepSeek、通义千问、Kimi、智谱 GLM 和硅基流动配置模板
- 支持从环境变量间接读取密钥并将配置文件加入 Git 忽略规则
- 未配置密钥时保持离线演示模式

### v0.4.1 - 2026-06-21

- 将论文选区状态与编辑区外部控件的浏览器焦点分离
- 切换写作目标、点击润色或结果按钮时保持原选区
- AI 润色和替换直接使用锁定选区，不再重新读取失焦后的原生选区
- 仅在用户重新操作编辑器时更新或取消原选区
- 编辑器收到新的指针操作时先释放旧选区，随后允许单击定位或拖动建立新选区

### v0.4 - 2026-06-21

- LaTeX 长行按编辑区当前宽度自动软换行
- 编辑区宽度变化时实时重新排版，不修改源文件换行
- 行号高度根据视觉换行动态计算并保持对齐
- 新增文件树、编辑区、AI 控制区和结果区拖拽调整宽度
- 自动保存并恢复用户调整后的布局宽度

### v0.3.1 - 2026-06-21

- 修复打开 Tex 文件后自动滚动到文末的问题
- 为每个文本文件持久化纵横滚动位置、光标和选区
- 切换文件或重新启动应用时恢复上次编辑视图
- 没有历史视图状态的新文件默认从文件开头打开

### v0.3 - 2026-06-21

- 使用 IndexedDB 持久化完整论文项目、当前文件和资源
- 应用启动时自动恢复上次打开的项目
- 文本修改后 600 ms 自动保存并显示真实保存状态
- 通过目录读写授权打开的项目支持自动写回原文本文件
- ZIP、拖拽和普通文件导入项目自动保存到浏览器本地缓存

### v0.2.1 - 2026-06-21

- 修复长篇 `.tex` 文件撑开编辑区域、无法使用滚轮上下移动的问题
- 为编辑器、项目树、AI 控制面板和结果区建立独立滚动容器
- 支持在行号栏使用滚轮控制正文滚动

### v0.2 - 2026-06-21

- 支持文件夹、ZIP 和单文件论文项目导入
- 新增项目文件树、BibTeX 编辑、PDF 和图片预览
- 新增 macOS 双击启动方式
